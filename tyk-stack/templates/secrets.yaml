apiVersion: v1
kind: Secret
metadata:
  name: secrets-tyk-stack
  namespace: {{ .Release.Namespace}}
  labels:
    app: secrets-tyk-stack
    release: {{ .Release.Name }}
type: Opaque
stringData:
  APISecret: "{{ .Values.global.secrets.APISecret}}"
