apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tyk-mdcb.fullname" . }}-test-connection"
  labels:
    {{- include "tyk-mdcb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tyk-mdcb.fullname" . }}:{{ .Values.mdcb.service.port }}']
  restartPolicy: Never
