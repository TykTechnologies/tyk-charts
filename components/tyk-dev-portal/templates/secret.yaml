{{- if not .Values.global.secrets.useSecretName  -}}
apiVersion: v1
kind: Secret
metadata:
  name: secrets-{{ include "tyk-dev-portal.fullname" . }}
  labels:
    app: secrets-data-{{ include "tyk-dev-portal.fullname" . }}
    chart: {{ include "tyk-dev-portal.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  DeveloperPortalLicense: "{{ .Values.license }}"
  adminUserPassword: "{{ .Values.global.adminUser.password }}"
  DeveloeprPortalStorageConnectionString: "{{ .Values.storage.database.connectionString }}"
  DeveloperPortalAwsAccessKeyId: "{{ .Values.storage.s3.awsAccessKeyid }}"
  DeveloperPortalAwsSecretAccessKey: "{{ .Values.storage.s3.awsSecretAccessKey }}"
{{- end }}
