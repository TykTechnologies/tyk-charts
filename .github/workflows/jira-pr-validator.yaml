name: Validate PR against Jira

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

concurrency:
  group: jira-validator-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  dep-guard:
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    steps:
      - name: Check for dependency changes
        id: check
        run: echo "skip=false" >> "$GITHUB_OUTPUT"

  validate:
    needs: [dep-guard]
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    # pull-requests: write is required for the action's sticky-comment step to post
    # linter feedback on the PR. Without it the step fails with
    # "Resource not accessible by integration" and hides the real linter error.
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Validate Jira ticket
        uses: TykTechnologies/jira-linter@38a9cabef56171c4e52ea698fa7be3db5fca3a49  # main
        with:
          jira-base-url: 'https://tyktech.atlassian.net'
          jira-api-token: ${{ secrets.JIRA_TOKEN }}
